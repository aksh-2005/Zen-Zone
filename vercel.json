{
  "buildCommand": "pip install -r requirements.txt --break-system-packages && cd zz && python manage.py collectstatic --noinput && python manage.py migrate",
  "outputDirectory": "zz/staticfiles",
  "framework": null
}